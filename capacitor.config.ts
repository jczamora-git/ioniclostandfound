import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ioniclostandfound',
  appName: 'IonicLostAndFound',
  webDir: 'dist'
};

export default config;
